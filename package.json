{
  "name": "dsrmap",
  "version": "0.1.0",
  "private": true,
  "description": "DSAR webform simulator and workflow analyzer",
  "scripts": {
    "dev": "vite --host --port 4173",
    "build": "vite build",
    "preview": "vite preview --host --port 4173"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}

